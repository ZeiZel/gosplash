// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: gosplash/media/v1/media.proto

// Пакет обязан совпадать с путём к файлу: proto/gosplash/media/v1 →
// gosplash.media.v1. Про префикс gosplash см. комментарий в events.proto.

package mediav1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	MediaService_GetPhoto_FullMethodName = "/gosplash.media.v1.MediaService/GetPhoto"
)

// MediaServiceClient is the client API for MediaService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ─────────────────────────────────────────────────────────────────────────────
// MediaService — синхронный API media-сервиса.
//
// gRPC здесь потому, что вызывающему нужен ответ ПРЯМО СЕЙЧАС: catalog получил
// из Kafka факт «фото загружено» и идёт за подробностями. Через Kafka такое
// делать нельзя — это был бы request-reply через очередь, а это боль.
//
// Правило проекта: gRPC — вопрос или команда с ответом, Kafka — факт без ответа.
// ─────────────────────────────────────────────────────────────────────────────
type MediaServiceClient interface {
	// Карточка одного фото. user_id в запросе не случайно: таблица photos
	// шардирована по user_id, и без него сервис не знает, на какой из баз
	// искать. Это первое, что меняется в API после шардирования, — ключ
	// шардирования приходится тащить во все запросы.
	GetPhoto(ctx context.Context, in *GetPhotoRequest, opts ...grpc.CallOption) (*GetPhotoResponse, error)
}

type mediaServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewMediaServiceClient(cc grpc.ClientConnInterface) MediaServiceClient {
	return &mediaServiceClient{cc}
}

func (c *mediaServiceClient) GetPhoto(ctx context.Context, in *GetPhotoRequest, opts ...grpc.CallOption) (*GetPhotoResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetPhotoResponse)
	err := c.cc.Invoke(ctx, MediaService_GetPhoto_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// MediaServiceServer is the server API for MediaService service.
// All implementations must embed UnimplementedMediaServiceServer
// for forward compatibility.
//
// ─────────────────────────────────────────────────────────────────────────────
// MediaService — синхронный API media-сервиса.
//
// gRPC здесь потому, что вызывающему нужен ответ ПРЯМО СЕЙЧАС: catalog получил
// из Kafka факт «фото загружено» и идёт за подробностями. Через Kafka такое
// делать нельзя — это был бы request-reply через очередь, а это боль.
//
// Правило проекта: gRPC — вопрос или команда с ответом, Kafka — факт без ответа.
// ─────────────────────────────────────────────────────────────────────────────
type MediaServiceServer interface {
	// Карточка одного фото. user_id в запросе не случайно: таблица photos
	// шардирована по user_id, и без него сервис не знает, на какой из баз
	// искать. Это первое, что меняется в API после шардирования, — ключ
	// шардирования приходится тащить во все запросы.
	GetPhoto(context.Context, *GetPhotoRequest) (*GetPhotoResponse, error)
	mustEmbedUnimplementedMediaServiceServer()
}

// UnimplementedMediaServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedMediaServiceServer struct{}

func (UnimplementedMediaServiceServer) GetPhoto(context.Context, *GetPhotoRequest) (*GetPhotoResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetPhoto not implemented")
}
func (UnimplementedMediaServiceServer) mustEmbedUnimplementedMediaServiceServer() {}
func (UnimplementedMediaServiceServer) testEmbeddedByValue()                      {}

// UnsafeMediaServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to MediaServiceServer will
// result in compilation errors.
type UnsafeMediaServiceServer interface {
	mustEmbedUnimplementedMediaServiceServer()
}

func RegisterMediaServiceServer(s grpc.ServiceRegistrar, srv MediaServiceServer) {
	// If the following call panics, it indicates UnimplementedMediaServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&MediaService_ServiceDesc, srv)
}

func _MediaService_GetPhoto_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetPhotoRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(MediaServiceServer).GetPhoto(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: MediaService_GetPhoto_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(MediaServiceServer).GetPhoto(ctx, req.(*GetPhotoRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// MediaService_ServiceDesc is the grpc.ServiceDesc for MediaService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var MediaService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gosplash.media.v1.MediaService",
	HandlerType: (*MediaServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "GetPhoto",
			Handler:    _MediaService_GetPhoto_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "gosplash/media/v1/media.proto",
}
