<!--
  Шаблон результата нагрузочного прогона (docs/perf/README.md).
  Скопируй в docs/perf/ГГГГ-ММ-ДД-<сценарий>[-<вариант>].md и заполни.
  Числа НЕ выдумывать: если поле нечем заполнить — прогон не готов
  к записи в docs/perf, а не "напишем примерно".
-->

# <сценарий> — ГГГГ-ММ-ДД

- **Сценарий:** `deploy/k6/<файл>.js`
- **Дата и время прогона:** ГГГГ-ММ-ДД ЧЧ:ММ (таймзона)
- **Что изменено с прошлого прогона:** <код / конфиг / инфраструктура /
  "ничего, контрольный повтор" — не оставлять пустым>
- **Стенд:** `make up` + <какие сервисы запущены: run-media/run-catalog/...>,
  железо/ресурсы, если отличаются от обычных (например, `docker update --cpus`)
- **Команда запуска:** `make k6-... MODE=...` (точная команда с параметрами)

## Числа

| Метрика | Значение |
|---|---|
| RPS | |
| p50, мс | |
| p95, мс | |
| p99, мс | |
| доля ошибок (`http_req_failed`) | |
| <кастомная метрика сценария 1, напр. `rate_limited`> | |
| <кастомная метрика сценария 2, напр. `idempotency_violations`> | |

<Вставь сюда JSON из `handleSummary` целиком — не пересказывай числами
из таблицы выше, таблица для беглого чтения, JSON — источник истины.>

```json
{
  ...
}
```

## Критерий готовности (docs/PLAN.md)

- **Какой именно критерий проверяет этот прогон:** <цитата из PLAN.md>
- **Подтверждён?** да / нет / частично — <почему>

## Наблюдения в Grafana

<Что видно на дашборде «gosplash overview» во время прогона — RED-метрики,
outbox, лаг консьюмеров. Расхождение с числами k6, если было, — тоже сюда.>

## Аномалии и заметки

<Всё, что не влезло выше: неожиданное поведение, подозрения на bottleneck,
что стоит перепроверить в следующем прогоне.>
