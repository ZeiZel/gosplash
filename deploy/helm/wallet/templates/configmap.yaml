apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wallet.fullname" . }}-config
  labels:
    {{- include "wallet.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
