{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "analytics.serviceAccountName" . }}
  labels:
    {{- include "analytics.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# automountServiceAccountToken: false — analytics не ходит в Kubernetes API
# (в отличие от, скажем, оператора или sidecar-инжектора), поэтому токен
# API-сервера ему не нужен вообще, а его наличие в файловой системе пода —
# лишняя цель для эксфильтрации при компрометации процесса.
automountServiceAccountToken: false
{{- end }}
