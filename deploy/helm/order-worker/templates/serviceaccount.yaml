{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "order-worker.serviceAccountName" . }}
  labels:
    {{- include "order-worker.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# automountServiceAccountToken: false — order-worker не ходит в Kubernetes API
# (в отличие от, скажем, оператора или sidecar-инжектора), поэтому токен
# API-сервера ему не нужен вообще, а его наличие в файловой системе пода —
# лишняя цель для эксфильтрации при компрометации процесса.
automountServiceAccountToken: false
{{- end }}
