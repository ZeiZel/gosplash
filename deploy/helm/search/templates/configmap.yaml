apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "search.fullname" . }}-config
  labels:
    {{- include "search.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
