apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "thumbnail-worker.fullname" . }}-config
  labels:
    {{- include "thumbnail-worker.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
